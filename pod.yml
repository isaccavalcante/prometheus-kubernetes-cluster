---
 apiVersion: v1
 kind: Pod
 metadata:
   name: my-first-pd
   
 spec:
   containers:
     - name: prometheus
       image: prom/prometheus
       ports:
         - containerPort: 9090
     - name: grafana
       image: grafana/grafana
       ports:
         - containerPort: 3000